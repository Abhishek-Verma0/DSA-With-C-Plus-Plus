/***  there are multiple operartions that we can perform  on queue 
 * enqueue -- inserts new elements in the last of queue if queue is not empty it creates node and add
 * dequeue --  removes the very first elemnets from the queue if empty then returns underflow
 * PEEK (front )--  returns the first element of the queue
 * isfull -- checks whether the queue is full or not returns the boolean value 
 * isempty  / isNull  --  checks the whether queue is empty or not 
 * 

 */